import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jessimaux.tish',
  appName: 'mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
